# FastAPI and server
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# Background tasks
celery[redis]==5.3.6
redis==5.0.1

# Fuzzy matching
rapidfuzz==3.6.1

# Database — httpx 0.28+ supports proxy kwarg that gotrue 2.9+ uses
supabase==2.3.5
httpx==0.28.1

# HTTP client for CRM APIs (version set above for supabase compat)

# Encryption
cryptography==42.0.2

# Environment and config
python-dotenv==1.0.1
pydantic==2.6.1
pydantic-settings==2.1.0

# Testing (dev only - compatible versions)
pytest==7.4.4
pytest-asyncio==0.23.4

# PDF generation
weasyprint==60.2
