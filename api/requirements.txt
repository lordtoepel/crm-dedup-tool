# FastAPI and server
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# Background tasks
celery[redis]==5.3.6
redis==5.0.1

# Fuzzy matching
rapidfuzz==3.6.1

# Database — pin gotrue to avoid proxy kwarg incompatibility with httpx
supabase==2.3.0
gotrue==2.8.0
httpx==0.26.0

# HTTP client for CRM APIs (version set above for supabase compat)

# Encryption
cryptography==42.0.2

# Environment and config
python-dotenv==1.0.1
pydantic==2.6.1
pydantic-settings==2.1.0

# Testing (dev only - compatible versions)
pytest==7.4.4
pytest-asyncio==0.23.4

# PDF generation
weasyprint==60.2
